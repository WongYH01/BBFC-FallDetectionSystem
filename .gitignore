*.pdf

# Data and experiment outputs — large, regenerable, not source.
data/
runs/
videos/
examples/

# Model weights (pretrained YOLO checkpoints download on demand, and the
# deployment pair comes from Hugging Face via scripts/fetch_weights.py)
*.pt
*.onnx
*.npz

# The research side of the project, which this repo does not carry: it tracks
# the deployment subset — fallcore, demo-cam, and the two scripts that stand a
# deployment up. Training, dataset preparation and the analysis notebooks live
# in the working tree but are not part of what ships.
notebooks/
fallcore/picam2.py
scripts/download_upfall.py
scripts/extract_omnifall_pose.py
scripts/prepare_upfall.py
scripts/render_picam.py
scripts/train_augmented_single.py

# Environments
.venv*/
demo-cam/.venv/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.ruff_cache/
